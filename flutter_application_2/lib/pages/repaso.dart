import 'package:flutter/material.dart';

class RepasoExamen extends StatelessWidget {
  const RepasoExamen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: const Center(
        child: Text('Repaso Examen'),
      ),
    );
  }
}